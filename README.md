# Carum Research Website (React + Vite + Tailwind)

## Local preview
1) Install Node.js (LTS)
2) In this folder:
   - npm install
   - npm run dev

## Build for production
- npm run build
This creates /dist (static files).

## Deploy
Upload the contents of /dist to your hosting provider (or connect to Vercel/Netlify).
